# AI 动态 MBTI 人格测试系统 — 项目简介（答辩材料）

> 软件综合训练（深度学习方向）团队作品
> 关键词：大语言模型应用 · 自适应测验 · 全栈开发

---

## 一、一句话介绍

**传统 MBTI 测试给所有人发同一套题；我们让 AI 实时分析你的每一道回答，动态生成下一道只属于你的问题，测得更快、更准，并沉淀为可追踪的性格档案。**

---

## 二、项目背景与动机

- MBTI 是全球最流行的人格测评工具，但传统测试存在明显缺陷：
  1. **题库固定**——所有人做同样的 28/93 道题，容易"猜出测什么"而失真
  2. **一刀切**——不管你某个维度已经多明确，仍然机械地平均分配题量
  3. **测完即弃**——结果是静态报告，无法追踪性格随时间的变化
- 大语言模型（LLM）的成熟让"动态出题 + 语义分析"成为可能，我们据此设计了本系统。

## 三、核心功能（按用户流程讲）

1. **注册登录**：账号体系，每个人的测试数据独立归属
2. **对话式自适应测试**：
   - AI 生成第一题 → 用户作答 → AI 分析 → 针对最不确定的维度生成下一题
   - 支持两种作答方式：**预设选项**（评分确定）与**自由文字回答**（AI 语义分析，信息量更大——这是传统测试做不到的）
   - 界面右侧实时显示四个维度的"测量进度条"，用户能直观看到 AI 在测什么、还差多少
3. **AI 人格报告**：MBTI 类型 + 中文昵称、150 字人格画像、核心优势、潜在短板、职业方向、同类型名人、个性化成长建议
4. **历史与性格变化曲线**：多次重测后，四维度得分随时间变化的折线图——把"测一次就扔"变成"持续追踪的性格档案"

## 四、技术架构（三件套对应课程要求）

| 课程要求 | 我们的实现 | 说明 |
|---|---|---|
| 人工智能算法 | 智谱 GLM-4-Flash 大模型 API + **自研自适应测试引擎** | 类似计算机化自适应测验（CAT）的简化实现 |
| 数据库 | SQLAlchemy ORM + SQLite/MySQL 双支持 | 6 张表：用户、测试会话、题目、回答、维度状态、报告 |
| 用户界面 | Vue 3 + Element Plus + ECharts | 对话式答题页、结果页、历史趋势页 |

**前后端分离**：FastAPI（Python）后端提供 RESTful API，前端通过代理访问。

**LLM 接口层**：采用 OpenAI 兼容协议，一行配置可切换智谱/Qwen/DeepSeek 等任意大模型；内置**模拟模式**（离线题库+模板报告），断网也能演示——答辩零风险。

## 五、自适应算法详解（技术亮点，重点讲）

系统为四个维度（E/I、S/N、T/F、J/P）各维护两个状态量：

- **score（倾向得分）**：-8 到 +8，负值偏第一字母、正值偏第二字母
- **confidence（置信度）**：已收集证据的可靠程度，目标 5.0

每轮循环：

1. **选题策略**：在置信度未达标的维度中，选出**置信度最低**者（排除上一题刚测过的维度），作为下一题的考察目标
2. **AI 出题**：LLM 按该维度生成一道生活情境选择题；四个选项在生成时即带有隐藏的倾向值（-2~-+2），**只存服务端、不下发前端**——用户无法通过选项位置猜出答案倾向
3. **双通道评分**：选选项→直接采用隐藏倾向值（确定、零成本）；自由回答→LLM 语义评分（-2~+2 + 判断理由）
4. **状态更新**：score 累加证据、confidence 增加 1.5（选项）或 1.2（自由回答）
5. **收敛判定**：四维度置信度全部达标（或达 20 题上限）→ 按得分符号确定 MBTI 类型 → LLM 生成个性化报告

**为什么这个设计是"更聪明"的？**
- 用户的明确维度少问、模糊维度多问——最少 **16 题**（4 维度 × 4 题）即可收敛，而不是固定题数
- 回答越一致收敛越快、回答矛盾则自动加测——题数本身就在响应用户
- **维度分数由引擎计算，不信任 LLM 编数字**——AI 只负责出题和写文案，量化决策权在算法手里，保证结果可复现

## 六、数据库设计（6 张表）

```
users（用户）───< test_sessions（测试会话）───< questions（题目）───< answers（回答）
                        │
                        ├───< dimension_states（维度状态，每会话 4 行）
                        └───< reports（报告，一会话一份）
```

- 每道题的**选项倾向值加密存储于服务端**（前端只拿选项文字）
- 回答表同时记录 evidence（证据值）与 AI 的判断理由，全程可审计
- 维度状态表沉淀每一步的 score/confidence 变化——这是"测量过程透明化"的体现

## 七、项目优点（PPT 直接可用）

1. **选题新颖**："AI 动态出题"是 LLM 时代才有的新物种，传统 Web 项目做不到，答辩辨识度高
2. **AI 融入深入**：不是"调一次 API"的贴皮 AI——出题、语义评分、报告生成三个环节深度使用大模型，且有一套确定性算法框架约束 LLM（结构化 JSON + 校验重试 + 分数自算），体现了工程化思维
3. **数据库价值真实**：不是"为了有数据库而存数据"——性格变化曲线、历史报告、测量过程审计，都只有在数据库支撑下才能实现
4. **用户体验完整**：对话式界面、实时收敛进度条、分阶段等待提示、雷达/折线可视化，细节考虑到了"AI 响应需要 7-8 秒"的等待体验
5. **工程规范**：前后端分离、RESTful API、JWT 认证、密码哈希加盐存储、ORM 抽象数据库（SQLite↔MySQL 一行切换）、.env 配置管理、.gitignore 排除敏感信息
6. **可靠性兜底**：LLM 不可用时自动降级为内置模拟模式，答辩现场断网/限流均不翻车
7. **可扩展性**：OpenAI 兼容协议意味着随时可换成更强的模型（Qwen-VL、GPT 等）；题量上限、收敛阈值等参数均集中在引擎中，可调可实验

## 八、演示流程建议（5 分钟）

1. **注册新账号**（10s）——展示账号体系
2. **开始测试，答 2-3 题**（1 分钟）——重点指着右侧进度条讲：**"看，AI 已经确定我在 E/I 维度的倾向了，它在追问我最不确定的维度"**；其中用一次**自由文字回答**，展示 AI 语义分析
3. **答完全部 16 题**（快进或提前答好）——展示自动收敛、生成报告
4. **结果页**（1 分钟）——人格画像、四维度倾向条、优势/职业建议
5. **历史页**（1 分钟）——**重测的性格变化曲线**，这是本项目最有"数据库价值"的画面
6. **收尾**（30s）——回到架构图：AI 算法（自适应引擎+LLM）、数据库（6 表）、界面（Vue3）三件套如何协作

**Q&A 预备**：
- "深度学习在哪？"→ 大模型本身就是深度学习最前沿的产物；我们的自适应引擎借鉴了心理测量学中 CAT（计算机化自适应测验）的信息量选题思想
- "AI 出的题可信吗？"→ 选项倾向值由服务端校验（-2~+2 越界即截断）、维度分数由确定性算法计算而非 LLM 自报，AI 只影响"题目措辞"不影响"测量标尺"
- "为什么 16 题就结束？"→ 收敛即停：4 维度各 4 题证据足够，这是"动态"的体现而非偷工减料

## 九、团队分工建议

| 模块 | 内容 |
|---|---|
| 后端 + 算法 | FastAPI、自适应引擎、LLM 接入、数据库设计 |
| 前端 | Vue3 四个页面、ECharts 可视化、交互细节 |
| 测试 + 文档 | 冒烟测试脚本、多账号多轮测试数据、README、答辩 PPT |

## 十、技术栈清单

前端：Vue 3 · Vite · Element Plus · ECharts · Axios · Vue Router
后端：Python · FastAPI · SQLAlchemy · PyJWT · OpenAI SDK
数据库：SQLite（开发）/ MySQL（部署）
AI：智谱 GLM-4-Flash（OpenAI 兼容协议，可切换 Qwen/DeepSeek/GPT）
